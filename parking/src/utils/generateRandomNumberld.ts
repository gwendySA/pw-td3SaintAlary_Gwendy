// refait pour que la BDD marhce
//export function generateRandomNumberId(): string {
 //const letters = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';  // Les lettres utilisées pour l'ID
// let id = '';
// for (let i = 0; i < 1; i++) {  // Génère un ID aléatoire d'une lettre (modifiable)
 // id += letters.charAt(Math.floor(Math.random() * letters.length));
// }
 //return id;
//}
